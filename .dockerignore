# Never bake real secrets into the image
.env
.env.*

# Generated at runtime, not needed in the image itself
articles/
manifest.json

# VCS / tooling noise
__pycache__/
*.pyc
.venv/
venv/

# Docs, not needed inside the image
*.md
*.pdf